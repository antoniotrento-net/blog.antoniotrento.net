# Claude — blog.antoniotrento.net

Indice di tutte le guide, CSV, Drip publish e **azioni Search Console** (blog nuovo + delistare la fattoria):

**`README.md`**

Prima di creare o modificare `_posts/`:

**`_piano_editoriale/COME-SCRIVERE-I-POST.md`**

Date = `date_file` in `_data/editorial_plan.yml`, non oggi. Prompt = CSV in `_piano_editoriale/` (questo repo).

Le pillar **non** si accendono da sole (Drip non le tocca). Dopo un lotto di post: apri `it/pillar/{chiave}` — stub o viva? Se il cluster è pieno, mettila in index (playbook **sezione 9**).

**Coda Sezione 1 (togli `sitemap: false` IT+EN, stesso giorno, push):** documenti-flussi **12 set 2026** · web-prodotto **13 set** · comprare-software **16 set** · prenotazioni-agenda **17 set**. Tabella e spunte: playbook sez. 9.
